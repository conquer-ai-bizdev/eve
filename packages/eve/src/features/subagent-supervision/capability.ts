/** Public contract revision implemented by this Eve build. */
export const SUBAGENT_SUPERVISION_CAPABILITY = 2 as const;

/** Largest descendant wait duration accepted by the runtime. */
export const SUBAGENT_MAX_WAIT_TIMEOUT_MS = 10 * 60_000;
