---
"eve": patch
---

Generate a path-independent behavior revision for every local agent node, including the shared package-manager dependency environment, and expose current-node identity through runtime callback context and agent inspection.
